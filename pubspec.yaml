import 'package:shared_preferences/shared_preferences.dart';
name: kitchen_inventory_app
description: A kitchen inventory management app
publish_to: 'none'
version: 1.0.0+1
SharedPreferences? prefs;

@override
void initState() {
  super.initState();
  initPrefs();
}

void initPrefs() async {
  prefs = await SharedPreferences.getInstance();
  await prefs?.setString('store_items', jsonEncode(jsonList));
}
final String? data = prefs?.getString('store_items');
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  uuid: ^4.3.3
  intl: ^0.18.1
dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
